[package]
name = "storage"  # 中文注释：存储层 crate 名称
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    ".",  # 中文注释：表示当前目录为存储层唯一的 crate
]

[dependencies]
core = { path = "../core" }  # 中文注释：依赖核心层