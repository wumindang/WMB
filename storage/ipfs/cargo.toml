[package]  # 中文注释：定义 IPFS 存储模块为库 crate
name = "ipfs"        # 中文注释：IPFS 存储模块名称
version = "0.1.0"    # 中文注释：版本号
edition = "2021"     # 中文注释：Rust 版本

[dependencies]
core = { path = "../../core" }  # 中文注释：依赖核心层，提供 IPFS 功能
tokio = { version = "1.0", features = ["full"] }  # 中文注释：异步支持