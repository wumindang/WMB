[workspace]  # 中文注释：定义安全层工作空间，管理多个安全模块
members = [
    "basic",      # 中文注释：基础安全模块，提供 ECDSA 签名和加密
    "threshold",  # 中文注释：门限签名模块，提供分布式签名功能
    "mfa"         # 中文注释：多因子认证模块，提供密码+验证码验证
]

[dependencies]  # 中文注释：共享依赖
core = { path = "../core" }  # 中文注释：依赖核心层