[package]
name = "wmb_send_tx"  # 中文注释：交易发送工具名称
version = "0.1.0"
edition = "2021"

[dependencies]
wmb_interface = { path = "../../interface" }  # 中文注释：依赖接口层
clap = { version = "4.0", features = ["derive"] }  # 中文注释：命令行解析库
tokio = { version = "1.0", features = ["full"] }  # 中文注释：异步运行时